# Capricorn V1.0.4

中文发布说明：[v1.0.4.zh-CN.md](v1.0.4.zh-CN.md)

English release notes: [v1.0.4.en.md](v1.0.4.en.md)

---

## 中文

Capricorn V1.0.4 继续沿用 `Version = 1.0`、`Build = 4` 的版本命名原则，因此 Display Name 显示为 `Capricorn V1.0.4`。本版重点提升测速、实时负载、磁盘刷新和命令执行的并发可靠性，并完成设置页、响应式布局、持久化边界和工程结构升级。

### 下载

- Release 包名：`Capricorn-v1.0.4-macOS.zip`
- App 名称：`Capricorn V1.0.4.app`
- 系统要求：macOS 14.0 或更新版本

### 重点更新

- 测速和实时负载使用带 UUID 的 `idle`、`running`、`stopping` 会话状态；停止操作会等待进程、文件句柄和临时文件真正清理完成，并拒绝旧任务的晚到事件。
- 使用 `AsyncStream` 在后台任务和主界面之间传递进度与结果，Darwin I/O 共享状态由锁保护，跨任务数据补充 `Sendable` 边界。
- 重写可取消命令执行层：每次运行独占一个子进程，取消时先终止再中断，并区分启动失败、非零退出和用户主动取消。
- 新增统一磁盘刷新服务：磁盘信息和 SMART 查询采用最多 4 路受控并发，每次刷新只执行一次 `smartctl` 扫描，重叠刷新时只有最新结果生效。
- 将应用拆分为 App、Domain、Features、Services、Persistence 和 Support 模块，并使用 macOS 14 的 Observation 状态模型。
- SwiftData 历史记录加入版本化 Schema、迁移计划、容器工厂和统一 Repository；保存和导出错误不再被静默忽略。
- 新增原生设置页，可配置语言、虚拟磁盘、报告序列号、普通 Tab 切页和自定义 `smartctl` 路径，设置内容完整支持简体中文。
- 新增 `Command-P` 打开设置，并在侧边栏左下角提供设置入口；`Control-Tab` / `Control-Shift-Tab` 始终可用于切换功能页。
- 控制栏、指标卡和宽表格改为响应式布局，最小窗口约为 1050×680。
- 新增分类 Unified Logging、本地构建运行脚本和只执行构建/测试的 GitHub Actions 质量检查。
- 补充取消后的晚到事件、立即重启、重叠刷新、子进程终止和旧数据库迁移等回归测试。

### 安全提示

Capricorn 不提供擦除、分区或裸设备写入功能。磁盘检查以只读诊断为主；装载、卸载、推出、重命名和强制卸载会调用 macOS 系统工具。测速和写入类负载会对目标存储造成压力和写入磨损，请先确认目标路径和剩余空间。当前 Release 资产使用本地已构建 App 打包，未通过本仓库流程自动公证。

---

## English

Capricorn V1.0.4 keeps the version naming rule of `Version = 1.0` and `Build = 4`, so the public Display Name is `Capricorn V1.0.4`. This release focuses on concurrency and cancellation reliability across benchmarks, live workloads, drive refresh, and process execution, while upgrading Settings, responsive layout, persistence boundaries, and project structure.

### Download

- Release package: `Capricorn-v1.0.4-macOS.zip`
- App name: `Capricorn V1.0.4.app`
- Requirement: macOS 14.0 or later

### Highlights

- Benchmark and live-workload sessions now use UUID-scoped `idle`, `running`, and `stopping` states. Stop waits for process, file-handle, and temporary-file cleanup, while late events from superseded work are rejected.
- Progress and results flow through `AsyncStream`; lock-protected state covers synchronous Darwin I/O, and cross-task values have explicit `Sendable` boundaries.
- Reworks cancellable command execution so every run owns its child process, cancellation terminates then interrupts it, and launch failure, non-zero exit, and user cancellation are distinct errors.
- Adds a unified drive refresh service with up to four concurrent disk-information and SMART tasks, one `smartctl` scan per refresh, and newest-refresh-wins semantics.
- Splits the application into App, Domain, Features, Services, Persistence, and Support modules using the macOS 14 Observation model.
- Adds a versioned SwiftData schema, migration plan, container factory, and repository boundary; save and export errors are no longer silently ignored.
- Adds native Settings for language, virtual-drive visibility, report serial numbers, plain-Tab navigation, and a custom `smartctl` path, with complete Simplified Chinese content.
- Adds `Command-P` for Settings plus a bottom-left sidebar entry; `Control-Tab` and `Control-Shift-Tab` always switch feature pages.
- Makes controls, metric cards, and wide tables responsive down to an approximately 1050×680 window.
- Adds categorized Unified Logging, a local build-and-run script, and build/test-only GitHub Actions quality checks.
- Expands regression coverage for late events after cancellation, immediate restart, overlapping refreshes, child-process termination, and legacy database migration.

### Safety Notes

Capricorn does not provide erase, partition, or raw-device write operations. Disk checks are primarily read-only diagnostics; mount, unmount, eject, rename, and force-unmount actions call macOS system tools. Benchmarks and write workloads can stress and wear the target storage, so confirm the target path and available capacity first. The release asset is packaged from the already-built local app and is not notarized by an automated repository workflow.

## Verification

Release verification completed on macOS:

- Five consecutive full test runs passed: 112 tests, 0 failures per run.
- Strict-concurrency build passed with compiler warnings treated as errors.
- Swift 6 compatibility build passed while the project remains on Swift 5 mode by default.
- The universal app bundle contains arm64 and x86_64 architectures and reports `Version = 1.0`, `Build = 4`.
- `script/build_and_run.sh --verify` rebuilt and launched the application successfully.
- Release ZIP SHA-256: `e3e27dbadcc9223bb7bc42c3fa98abb01401841e44382b3e7a0f39b800d3190d`.
- The bundle contains a hardened-runtime signature, but local trust evaluation reports `CSSMERR_TP_NOT_TRUSTED`; this release is not presented as notarized.
