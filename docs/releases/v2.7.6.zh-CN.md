# Capricorn V2.7.6 中文发布说明

发布日期：2026-09-21

双语发布说明：[v2.7.6.md](https://github.com/LexTheAries2209/Capricorn/blob/v2.7.6/docs/releases/v2.7.6.md)

Capricorn V2.7.6 使用 `Version = 2.7`、`Build = 6` 和 Display Name `Capricorn V2.7.6`。

## 主要更新

- App 作为 XCTest 宿主启动时，不再执行真实磁盘刷新、磁盘事件监控或 GitHub 更新检查，避免系统 I/O 与网络副作用阻塞测试进程。
- SMART 测试使用独立命令协调器，不再与全局运行队列共享状态，减少测试之间相互等待的可能。
- 为内置 `smartctl` 扫描、SMART 读取和版本查询分别设置 15 秒、30 秒和 10 秒超时；超时后会终止子进程并释放协调器队列。
- 网络卷和存储卡会在 `smartctl` 设备扫描前直接返回不支持，避免无意义的外部命令和 CI 环境等待。
- GitHub Actions 质量工作流仅在 `Main` 分支推送和拉取请求运行，不再因发布标签重复执行完整测试。
- 同一分支的新运行会取消旧运行；完整测试最长 20 分钟，严格并发和 Swift 6 兼容性构建最长 15 分钟。
- CI 测试显式关闭 App 启动副作用、关闭测试并行、启用无缓冲日志，并将单个测试限制为 60 秒，使停滞位置能快速失败并明确报告。

## 下载、兼容性与安全

- Release 包名：`Capricorn-v2.7.6-macOS.zip`
- App 名称：`Capricorn V2.7.6.app`
- 系统要求：macOS 14.0 或更新版本
- smartctl 7.5 已内置。
- SAT SMART Driver 是兼容 USB-SATA 设备的可选组件，不会被自动安装或加载。
- Capricorn 只在所选测速文件夹中创建临时文件，不直接写入裸设备。“仅读取”仍可能创建读取准备文件，不能视为零写入模式。
- 写入、混合、循环测速和实时活动大文件负载会产生写入与磨损，请先确认目标和可用空间。

## 验证

- 319 项 macOS 测试全部通过。
- 严格并发 Release 构建在警告即错误模式下成功；Swift 6 兼容性构建成功，仅有一项已有的弃用警告。
- Release Archive、内置 smartctl 验证和仓库启动验证均成功。
- App 版本元数据、arm64/x86_64 通用架构、内置资源和 dSYM UUID 已验证。
- ZIP 大小：9,383,908 字节
- SHA-256：`02553148d4016384c34ae3035a3d594563559991c1d2b7d970bdf967a1acdf9d`
- 本次 Archive 仅包含 ad-hoc 链接器签名，未执行 Developer ID 签名、公证和 Gatekeeper 信任验证，首次打开可能需要手动允许。
